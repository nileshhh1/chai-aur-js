"use strict"; //treat all js code as new version

console.log(3+3);

let age=9;
let isLoggedIn=false
let state= null // khali h

// number => 2 to the power 53
// bigint => use to store a very large number
// string => used to store multiple characters within single quote ' ', double qoute " ", and backtik ` `;
// undefined=> variable is declared but value is not initialised
// eg let a; output is undefined
// symbol=> unique

console.log(typeof null); // object
console.log(typeof undefined); //undefined
